# CI/CD Pipeline

The project uses GitHub Actions for continuous integration and automated testing.

## ⚙️ Workflows
- **`ci_qa_monitoring.yml`**: Compiles code, runs xUnit unit tests, and checks code quality on every push to `main`.
